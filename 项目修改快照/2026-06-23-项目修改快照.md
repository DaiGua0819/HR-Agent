# 2026-06-23 项目修改快照

## 面试总结严格模板与最近初复试回填

- 用户要求后续面试总结必须严格按照飞书模板结构生成，标题和章节需要与模板一致。
- 已读取模板文档 `https://rnftvujbd6.feishu.cn/docx/OGw0d6cT9o5LDXxdbePccZSgnwd` 的真实标题结构，并将面试总结生成逻辑固定为：
  - `Behavior question 部分`
  - `你问了什么、能测什么、哪里不够`
  - `这一部分暴露出来的问题`
  - `这部分建议怎么打分`
  - `Technical question 部分`
  - `逐题分析`
  - `能力画像（技术岗位视角）`
  - `AI 平台/工作流工程能力：中上`
  - `算法/检索增强理解：中上`
  - `工程稳定性与治理：中等`
  - `可培养性：中上`
  - `这轮“问题展开”里哪些地方有点问题`
  - `问得对的地方`
  - `可优化点`
  - `我会怎么给这个人打分（10分制）`
  - `综合结论`
  - `下一轮重点追问（直接可用）`
  - `Behavior 题`
  - `技术/业务场景题`
- 已移除旧版非模板章节，如 `总结`、`公司与求职者基本情况`、`项目技术交流`、`公司相关问题交流`、`初步评价`。
- 文档标题改为 `模板严格版：候选人初试/复试（日期）`。
- 已部署到服务器并重启 Web 服务，后续新生成的初面/复试总结会默认使用该严格模板。

### 已更新的最近面试总结

- 孙彦哲初试：`技能评价` 已更新为 `https://feishu.cn/docx/RKdmdHOZXohSfxxMvpHc66kTnmf`
- 张子杰初试：`技能评价` 已更新为 `https://feishu.cn/docx/XErddIyAgo0XorxITuTcIcCynex`
- 黄智豪初试：`技能评价` 已更新为 `https://feishu.cn/docx/OrzpdN7Szo8Gbjx8SC8cWKQonyd`
- 黄智豪复试：`复试结果评价` 已更新为 `https://feishu.cn/docx/P6JjdR7bbozQimxCvLgc4qAmnse`
- 孙彦哲复试：`复试结果评价` 已更新为 `https://feishu.cn/docx/QW0NdLjQwoUoDJxa37JcPZKgntf`

### 处理细节

- 孙彦哲初试原先缺少结构化回灌数据，已先完成回灌后再生成严格模板总结。
- 写回飞书多维表格时，保留了字段中原有手写评价文字，仅替换字段开头的旧 docx 链接。
- 初面统一写入 `技能评价` 字段，复试统一写入 `复试结果评价` 字段。
- 飞书字段类型确认为文本字段，最终以 `新 docx 链接 + 原手写评价` 形式写入。
- 清理了用于临时读取模板结构和批量刷新结果的公开 JSON 文件，避免会议纪要或模板结构结果留在静态目录。

### 验证

- `node --check patchwork-recruit-gpt/server/features/interview-center/feedbackBackfill.js` 通过。
- `node --check patchwork-recruit-gpt/server/features/interview-center/index.js` 通过。
- 线上 `http://218.244.142.84:8080/interview-center.html` 返回 200。
- 面试中心接口复查显示 5 份初面/复试总结均标记 `strictTemplate=true`，且对应字段链接已更新。

## 51 和新红账号联系人打开修复

- 问题现象：51 和新红在 24h 处理中多次只处理 1-3 人就显示完成，但日志仍有剩余未读和可处理消息。
- 服务器日志确认原因：点击联系人后，51 页面头部/选中联系人与刚点击的候选人不一致，触发防串人保护；随后 24h 两轮跑完后被显示为完成。
- 对比 51 宋峰峰：两账号使用同一套 51job 处理函数，宋峰峰能通过姓名/头部匹配；和新红页面更容易出现选中态或头部更新慢，所以更容易触发不一致。
- 修复范围：只修改 `agent_core/legacy_parts/agent_web_server.part005.py`，新增 `job51_is_hexinhong_runtime()`，仅当 `PORT=8791` 或 `AGENT_ACCOUNT_ID=job51_b` 时生效。
- 修复逻辑：
  - 和新红账号打开联系人时延长点击后的等待和聊天输入框等待时间。
  - 只对和新红恢复索引辅助确认：当列表选中索引与目标索引一致时，允许进入后续校验。
  - 后续真正处理前仍保留 `job51_read_chat_context` 的候选人身份告警，若发现头部/选中姓名不一致，只记为 `identity_mismatch_skipped` 并继续，不发送消息、不求简历、不下载到错误候选人。
- 部署：已覆盖服务器 `C:\RecruitAgent\project\agent_core\legacy_parts\agent_web_server.part005.py`；远程备份在 `C:\RecruitAgent\project\rollback_backups\job51_b_open_fix_20260623_134109\agent_core\legacy_parts\agent_web_server.part005.py`。
- 验证：
  - 本地 legacy parts 拼接编译通过，远程 `python -m py_compile agent_web_server.py` 通过。
  - 单独拉起 `RecruitAgentAgent8791Job51B` 后跑 51 和新红两轮。
  - 第 1 轮：后台任务完成，处理 40 人；动作统计 `accepted_resume_downloaded=30`、`accepted_resume_blocked=2`、`sent_position_screening=5`、`sent_basic_conditions=1`、`checked=1`、`identity_mismatch_skipped=1`。
  - 第 2 轮：后台任务完成，处理 40 人；动作统计 `accepted_resume_downloaded=35`、`accepted_resume_blocked=1`、`sent_position_screening=1`、`sent_basic_conditions=1`、`checked=1`、`identity_mismatch_skipped=1`。
  - 两轮均未再出现“当前聊天头部/选中联系人不一致后直接中断并显示完成”的情况。
- 收尾：测试后已关闭 8791 agent，保留 9225 浏览器/CDP。

## 51 和新红在线简历打开底层禁用

- 背景：51 和新红处理消息时，点击聊天里的“在线简历/已投简历”会被 51job 平台重定向到 `/Revision/talent/management`，导致不停打开人才管理页。用户要求从源头不再打开，而不是打开后再关闭。
- 代码修复：
  - `agent_core/legacy_parts/agent_web_server.part003.py`
    - 在 `job51_dom_click_online_resume_entry()` 入口增加 `job51_is_hexinhong_runtime()` 硬拦截。`PORT=8791` 或 `AGENT_ACCOUNT_ID=job51_b` 时，任何路径都不能再点击在线简历 DOM。
    - 在 `job51_open_online_resume_detail()` 入口增加同样硬拦截。即使上层误调用打开在线简历详情，也会直接返回 `online_resume_detail_open_disabled_to_prevent_talent_management`。
  - `agent_core/legacy_parts/agent_web_server.part004.py`
    - 保留上层下载分支拦截：51 和新红不走在线简历 PDF 保存，回落到当前聊天里的求简历流程。
  - `agent_core/legacy_parts/agent_web_server.part014.py`、`agent_core/legacy_parts/agent_web_server.part018.py`
    - 更新 functioncall/agent prompt 描述：`job51_b` 不允许打开或点击在线简历详情页，只能先查本地下载记忆，若没有则在当前聊天中求简历。
- 验证：本地 legacy parts 拼接编译通过；历史 SyntaxWarning 为既有转义 warning，本次未新增语法错误。
- 待验证：部署到服务器后，单独运行 51 和新红处理消息至少 150 人，并确认 CDP 标签页里不再新增 `/Revision/talent/management`。
