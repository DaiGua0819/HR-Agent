# 2026-06-22 项目修改快照

## 主动联系动作日志统计补齐

### 需求
- 今天 BOSS 主动联系实际跑了多人，但前端“点开人数”仍显示 0。
- 只修复前端统计口径，不改主动联系执行、筛选、发送、CDP、agent 或浏览器逻辑。

### 问题原因
- 今天的主动联系没有写入 `recruiter_batch_reports*.json` 批次汇总。
- 实际逐候选人动作写在 `recruiter_proactive_action_log*.jsonl`，字段包含 `createdAtTs`、`accountId`、`checkpoints`。
- 原统计 fallback 只读取批次汇总，不读取逐条动作日志，因此前端接口无法看到今天已点开的候选人。

### 修改
- `patchwork-recruit-gpt/server/legacy-parts/server.part002.js`
  - 新增 `recruiter_proactive_action_log*.jsonl` 统计 fallback。
  - 使用 `createdAtTs/updatedAtTs/actionLogId` 换算上海日期，支持按日期和账号过滤。
  - 识别 `open_resume_dialog/read_resume` 作为“点开人数”依据。
  - 识别 `decision=greeted` 或 `greet/click_greet` checkpoint 作为“主动打招呼”依据。
  - 按候选人 key 去重，再与原有明细统计、批次汇总统计做 max 合并，避免重复累加。

### 验证
- 本地语法检查通过：`node --check patchwork-recruit-gpt/server.js`。
- 已部署到服务器并重启 `RecruitAgentWeb8765`。
- 服务器首页本机访问返回 200。
- 服务器接口验证：
  - `/api/boss-automation/summary?accountId=all&date=2026-06-22&force=1` 返回 `proactiveOpened=16`、`proactiveGreeted=0`。
  - `/api/boss-automation/summary?accountId=boss_a&date=2026-06-22&force=1` 返回 `proactiveOpened=8`。
  - `/api/boss-automation/summary?accountId=boss_b&date=2026-06-22&force=1` 返回 `proactiveOpened=8`。
  - `/api/boss-automation/details?accountId=all&date=2026-06-22&force=1` 返回 `proactiveOpened=16`。

## 面试总结文档模板修复

### 需求
- 面试结束后的“技能评价”飞书总结文档需要严格参考“杨潇初试总结-2026-06-16”的结构。
- 张子杰、黄智豪两份已生成文档需要重新总结并覆盖正文。

### 修改
- `patchwork-recruit-gpt/server/features/interview-center/feedbackBackfill.js`
  - 将技能评价文档模板从 Behavior / Technical / 10 分制表格结构，调整为杨潇模板口径：
    - 总结
    - 公司与求职者基本情况
    - 项目技术交流
    - 公司相关问题交流
    - 初步评价
    - 综合结论
    - 下一轮重点追问
  - 总结内容继续基于飞书纪要、问题包和结构化回灌结果生成。
- `patchwork-recruit-gpt/server/features/interview-center/feishuClient.js`
  - 增加飞书文档正文覆盖能力：先清空根文档正文块，再写入新模板内容。
  - 增加 bullet / ordered block 写入支持，使要点列表在飞书文档里以项目符号和编号列表展示。
- `patchwork-recruit-gpt/server/features/interview-center/bitableAssets.js`
  - 强制回灌时允许重写已有技能评价 docx，不再因为已有链接直接跳过。
- `patchwork-recruit-gpt/server/features/interview-center/index.js`
  - 强制回灌时复用已有技能评价文档 ID，覆盖正文，并保持多维表格“技能评价”链接不变。

### 文档覆盖结果
- 张子杰
  - 文档：`https://feishu.cn/docx/Ge0tdb02uoevbuxCWilczKYpnJc`
  - 状态：已重新读取纪要、重新生成评价、已覆盖正文。
  - 回灌建议：补问。
- 黄智豪
  - 文档：`https://feishu.cn/docx/Dr0UdRV8xoTOvWx33kbcEGUDngw`
  - 状态：已重新读取纪要、重新生成评价、已覆盖正文。
  - 回灌建议：补问。

### 验证
- 服务器语法检查通过：
  - `node.exe --check feedbackBackfill.js`
  - `node.exe --check feishuClient.js`
  - `node.exe --check bitableAssets.js`
  - `node.exe --check index.js`
- 已重启 `RecruitAgentWeb8765`。
- 服务器本机 `http://127.0.0.1:8765/interview-center.html` 返回 200。
- 已读取两份 docx 的 `raw_content` 验证：
  - 均包含“公司与求职者基本情况 / 项目技术交流 / 初步评价 / 下一轮重点追问”。
  - 均不再包含旧模板中的 “Behavior question / Technical question / 10分制 / 逐题分析”。

### 备注
- 飞书 docx 正文已覆盖成功，文档 ID 和多维表格链接保持不变。
- 飞书文档标题仍保留原标题，当前已验证的 docx/drive 改名接口参数未通过，未继续冒险试错；正文模板已按要求修复。

## 约面试换微信状态识别补齐

### 需求
- 汤新华约面试时，BOSS 已点击“换微信”并确认，页面显示“请求交换微信已发送”，但系统没有继续发送“加我微信沟通”。
- 只补齐换微信成功态识别，不影响联系人搜索、换微信点击、确认弹窗、消息输入和发送等其他流程。

### 修改
- `agent_core/legacy_parts/agent_web_server.part018.py`
  - 扩展 `inspect_interview_wechat_exchange_state` 中的 `requestVisible` 判断。
  - 新增识别“请求交换微信已发送 / 交换微信请求已发送 / 已发送交换微信请求”这类页面状态。
  - 该函数为约面试换微信的公共状态校验逻辑，六个账号复用同一识别结果。

### 验证
- 服务器主入口语法检查通过：`python -m py_compile agent_web_server.py`。
- 已用本次失败样例中的 `09:09 请求交换微信已发送` 文本验证，新正则可识别为 `requestVisible=true`。
- 未改动实际点击、发送、候选人匹配、24h 自动处理逻辑。

## 主动联系前端统计修复

### 需求
- 主动联系功能本身正常，但前端“点开人数”经常显示为 0 或 1，和实际主动联系点开的候选人数不一致。
- 只修复前端统计数据，不影响主动联系、消息处理、CDP、agent、浏览器操作。

### 问题原因
- 主动联系报告会把点开人数写入 `state.counts.opened / openedCandidates`。
- 部分主动联系报告没有逐个候选人的 `results` 明细，例如真实报告里 `openedCandidates=5`、但 `results=[]`。
- Web 端发现本地深度记录后，优先按候选人明细去重统计，忽略了这类只有汇总 counts 的主动联系报告，导致“点开人数”显示偏低。

### 修改
- `patchwork-recruit-gpt/server/legacy-parts/server.part002.js`
  - 新增主动联系 count-only 报告汇总兜底。
  - 从 `recruiter_batch_reports*.json` 读取 `opened / openedCandidates / proactiveOpened` 和 `greeted / proactiveGreeted`。
  - 合并指标时采用 `max(明细统计, 报告汇总)`，避免重复叠加。
- `patchwork-recruit-gpt/server/legacy-parts/server.part000.js`
  - BOSS 摘要接口、通用平台摘要接口返回前合并主动联系汇总 counts。
  - 自动化详情页顶部指标同样合并主动联系汇总 counts。

### 验证
- 已用服务器真实报告验证：`results=[]` 但 `state.counts.opened=5/openedCandidates=5` 的主动联系报告，会补齐到前端 `proactiveOpened=5`。
- 主入口语法检查通过：`node --check patchwork-recruit-gpt/server.js`。
- 已部署到服务器并重启 `RecruitAgentWeb8765`。
- 服务器接口验证通过：
  - `/api/boss-automation/summary?accountId=boss_b&date=2026-06-18&force=1` 返回 `proactiveOpened=5`。
  - `/api/boss-automation/details?accountId=boss_b&date=2026-06-18&force=1` 返回 `proactiveOpened=5`。
- 未改动主动联系执行流程、筛选规则、发送动作、消息处理流程。
## BOSS 宋峰峰常用语发送修复

- 问题：BOSS 宋峰峰处理 AI应用开发实习生时，缓存的常用语发送坐标可能点偏，系统会误判为已点击发送，最终在聊天记录校验时发现话术没有出现并标记 blocked。
- 修复：
  - 对 BOSS 常用语缓存发送区域增加可信度校验，缓存区域异常过大时清空缓存并改走 DOM/hover 定位。
  - 缓存坐标点击后必须在当前聊天记录里验证目标话术新增，未验证成功不再视为 clicked。
  - 修通“右侧发送按钮未成功点击”后的 fallback，让系统继续点击常用语文本进入输入框并通过聊天发送按钮发送。
  - 修复 hover 分支 `found=true` 但 `clicked=false` 时提前返回的问题，避免跳过后续 DOM fallback。
- 影响范围：仅涉及 BOSS 常用语/基础情况话术发送链路；未改动 51、智联、面试中心、主动联系流程。

## 验证

- 已对 `agent_core/legacy_parts/agent_web_server.part*.py` 进行 legacy loader 拼接编译，结果通过。
