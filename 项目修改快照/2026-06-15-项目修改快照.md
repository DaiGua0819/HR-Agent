# 2026-06-15 项目修改快照


## 2026-06-15 15:10 约面试联系人桥接与演示状态

### 已完成变更
- 将约面试入口从“简历字段直接发起”调整为“简历库记录 -> 消息处理数据库联系人记录”的桥接逻辑。
- 新增 `findResumeContactBridge()` / `applyResumeContactBridge()`，优先通过 `platformContact/sourceKey/conversationKey` 关联消息处理阶段保存的联系人上下文。
- 桥接评分规则：`>=110` 视为高置信可用，`<25` 视为不可确认。
- 成功桥接后会回填 `platformContact.displayName`、`sourceKey`、`conversationKey`、`candidateIdentityKey` 和少量 `chatEvidence`。
- 前端简历库点击“约面试”时，优先使用桥接后的平台联系人显示名去平台搜索。
- 已提交 `5b05de9 实现约面试联系人桥接`，并推送到 GitHub `main`。

### 本地验证结果
- 读取本地 `/api/resumes` 的 2026-06-15 简历记录后，发现部分记录缺少 `platformContact/sourceKey`。
- 使用平台、账号、候选人姓名、文件名和消息处理数据库进行桥接模拟。
- 当天可识别平台来源的 28 份简历中，高置信匹配 28 份，失败 0 份。
- 今天简历库第一位候选人为 `杨子毅`，来源 `BOSS 和新红`，模拟桥接到 `boss_b`，评分 `235`。

### 服务器部署状态
- GitHub 最新代码已经到 `5b05de9`。
- 服务器尚未完成部署，原因是当前可用远程通道都无法执行命令：
  - SSH 22 端口可达，但 `Administrator` 密码认证会被服务端关闭。
  - Paramiko 返回 `Authentication failed: transport shut down or saw EOF`。
  - WinRM 5985 端口可达，但 NTLM/Basic 都被 reset/disconnect。
  - SMB 访问 `\\218.244.142.84\C$` / `ADMIN$` 返回 `System error 64`。
  - WMI/RPC 返回 `RPC server is unavailable`。
  - CDP 端口 9222/9230/9224/9225/9226/9231 TCP 可达，但 HTTP `/json/version` 超时，不能作为部署通道。
- 当前服务器仍需要通过 RDP 登录后手动 `git pull` 并重启服务，或者提供一个可用的远程命令通道。

### 演示安全边界
- 约面试演示必须避免真实发送消息，除非用户在执行当时明确确认。
- 当前 agent 的 `dryRun=true` 只会打开平台、搜索、校验联系人、定位聊天输入框。
- 当前 `dryRun=true` 不会把 `加我微信沟通，carhhxh` 填进输入框。
- 非 `dryRun` 分支会填入并发送消息，不能直接用于“填好但不发送”的演示。
- 若要演示“输入消息但停止发送”，需要新增 `draftOnly` / `prepareOnly` 模式：搜索 -> 校验 -> 点开会话 -> 填入消息 -> 不点击发送。
- 演示结束后如该模式只是临时功能，需要按用户要求删除临时代码并提交记录。
- 演示前必须确认目标账号 agent ready。最近一次检查中 `boss_b` 浏览器/CDP ready，但 agent 未 ready，`agentError=fetch failed`，所以不能直接演示。

### 待办
- 如需演示“输入但不发送”，先实现 `draftOnly` / `prepareOnly` 模式。
- 部署前需恢复可用的服务器命令通道，或由用户 RDP 到服务器执行拉取和重启。
- 演示前再次确认 `boss_b` 或目标账号的浏览器、CDP、agent 都处于 ready。

## 长期文档更新约定
- 后续每次代码修改、部署尝试、服务器状态检查、线上问题排查、安全边界和遗留问题，都必须同步写入 `项目修改快照/YYYY-MM-DD-项目修改快照.md`。
- 记录至少包含：改了什么、为什么改、涉及哪些文件、验证结果、Git 提交状态、是否已部署到服务器、还有哪些未解决问题。
- 不能只在聊天里说明关键结论；需要把可接力的信息沉淀到 MD。

## 2026-06-15 15:20 线上约面试按钮不可点击排查

### 现象
- 用户反馈前端简历库里的“约面试”仍然点击不了。

### 排查结果
- 线上 `http://218.244.142.84:8080/src/client/scoring/rules.js` 仍然是旧版本：
  - 不包含 `resumeNeedsInterviewContactBridge()`。
  - 不包含 `getResumePlatformContactDisplayName()`。
  - 仍然包含旧判断：没有 `platformCandidateId` 时返回“历史简历缺少平台ID，无法按ID搜索约面试”。
- 线上 `server/legacy-parts/server.part004.js` 也仍然是旧版本：
  - 不包含 `findResumeContactBridge()`。
  - 不包含按 `platformContact.displayName` 生成 `searchName` 的逻辑。
  - `resolveResumeInterviewTarget()` 仍然要求 `platformCandidateId`。
- 线上 `/api/resumes` 当前返回 691 条简历，统计结果：
  - `platformCandidateId`：0 条。
  - `platformContact`：0 条。
  - `platformContact.displayName`：0 条。
  - `sourceKey`：0 条。
  - `interviewInvite.status=sent`：0 条。
  - `interviewInvite.status=failed`：0 条。

### 根因
- GitHub 已经有 `5b05de9 实现约面试联系人桥接` 和 `6f66fed 补充长期文档更新约定`，但服务器 Web 项目没有部署到最新版本。
- 线上仍运行旧版“按平台ID约面试”逻辑，而当前简历库没有任何 `platformCandidateId`，所以按钮被旧前端保护性禁用。

### 处理方式
- 需要先把服务器 `C:\RecruitAgent\project\patchwork-recruit-gpt` 更新到 GitHub 最新 `main`，并重启 `RecruitAgentWeb8765`。
- 更新后按钮逻辑会从“必须有平台ID”变为：
  - 自动化平台来源可点击。
  - 已有 `platformContact.displayName` 时显示“约面试”。
  - 缺少 `platformContact.displayName` 但来源可识别时显示“查找联系人”，点击后由后端桥接消息处理数据库。
- 注意：当前远程命令通道仍不可用，不能声称已部署；需要 RDP 到服务器执行 `git pull` 和重启，或恢复可用 SSH/WinRM/SMB/WMI 通道。

## 约面试搜索依据调整

- 将简历库“约面试”能力从平台候选人 ID 搜索调整为平台联系人显示名搜索。
- 新增 `platformContact` 轻量元数据：
  - `displayName`：平台联系人当前使用的显示名，用于搜索。
  - `label`：联系人列表整行文本，用于人工排查。
  - `appliedPosition`：当时沟通岗位，用于同名联系人校验。
  - `chatEvidence`：最近 3 条有效聊天片段，用于辅助确认。
- 新简历下载记录会在 agent 侧保存 `platformContact`；Web 入库扫描会按 PDF 的 hash/path/filename 从下载索引回捞该字段并写入简历记录。
- 不再为新入库链路写入 `platformCandidateId` 或 `detailUrl`，历史字段仅保留兼容旧数据展示。

## 约面试执行保护

- 后端 `POST /api/resumes/:id/interview-invite` 改为要求 `platformContact.displayName`，缺失时按钮/接口均阻断。
- Agent `/api/interview-invite` 改为按 `searchName/platformContact.displayName` 搜索。
- 搜索后用联系人名、岗位、聊天证据和搜索结果数量做确认：
  - 同名多人且证据不足时返回 `multiple_candidates_unverified`。
  - 找不到可确认联系人时返回 `search_result_not_found`。
  - 岗位/聊天证据无法匹配时返回 `chat_evidence_not_matched`。
- 前端确认弹窗和按钮 tooltip 改为显示“平台联系人”，不再显示平台 ID。

## 文档

- 更新 `agent_skills/interview-scheduling/SKILL.md`，明确禁止用简历姓名、手机号、文件名、URL 或平台候选人 ID 兜底。
- 约面试固定话术保持为：`加我微信沟通，carhhxh`。

## 验证

- JS legacy server 分片拼接编译通过。
- Python agent legacy 分片拼接编译通过。
- 前端 `src/client/scoring/rules.js` 语法检查通过。

## 自动化统计日期与兜底修复

- 问题原因：
  - 24h 进度兜底会把 `target_completed/target_interrupted` 展开成候选人明细，且岗位固定写成“未识别岗位”，导致每日扇形图被兜底假记录污染。
  - 兜底进度只包含处理人数、求简历、获取简历，之前却被当成完整统计源使用，所以发话术、候选人提问、答疑等指标会被显示成不可靠的 0。
  - 日期逻辑允许“今天”读取前一天 36 小时内的 24h 日志；线上 `2026-06-15` 没有 24h 日志，却把 `2026-06-14` 的 53/21/9 处理数据算进了今天。
- 修复内容：
  - `automation24hTargetBelongsToDate` 改为严格按所选日期匹配，不再把前一天日志归到今天。
  - `automation24hLogDateKeys` 只读取当前选中日期的日志文件。
  - `/api/*-automation/details` 不再追加 `automation24hFallbackDetailRecords`，兜底只补充摘要 metrics，不生成候选人明细，也不参与岗位扇形图。
  - 每轮 24h 日志如果带有 `counts/resultMessage`，兜底摘要会尽量恢复发话术、求简历、获取简历、答疑等数字。
  - 前端每日扇形图额外过滤 `recoveredFrom24hStatus` 记录，作为防线。
- 服务器部署：
  - 备份目录：`C:\RecruitAgent\project\rollback_backups\stats_fallback_fix_20260615_102908`
  - 覆盖文件：`server.part001.js`、`server.part002.js`、`src/client/automation/overview.js`、`script.js`、`index.html`
  - 旧 8765 Node 子进程没有被 `schtasks /End` 杀掉，已按监听端口停止旧 PID，并通过 `RecruitAgentWeb8765` 拉起新 PID `19036`。
- 线上验证：
  - `GET /index.html?job=HRBP` 返回 200，脚本版本为 `20260615-stats-date-fix`。
  - `2026-06-15`：BOSS details 不再 recovered，明细 6 条、兜底假记录 0，岗位分布为 AI 应用开发实习生 5、人力资源管培生 1；51/智联今天没有 24h 兜底数据。
  - `2026-06-14`：24h 摘要仍可按昨天日期查看，且不再生成兜底假记录。

## 51 和新红误进人才管理、智联 24h 单轮 3 人排查

- 51 和新红问题：
  - 本地已修改 `agent_core/legacy_parts/agent_web_server.part002.py`、`part003.py`、`part005.py`。
  - 连接 51 CDP 时优先选择 `Revision/chat` 聊天页，并关闭误打开的 `Revision/talent/management` 页。
  - 51 打开聊天页时复用已有聊天页并 bring to front，避免留在人才管理页。
  - 51 点击联系人时优先点 `.username.at` 候选人姓名区域，找不到姓名时点列表左侧安全区，不再点整行中间，降低误触人才管理/简历入口概率。
- 智联只处理前几个人的原因：
  - 不是今天修 51 代码引起的；智联 agent 滚动函数本身没有被本次改动触碰。
  - 24h 调度里历史提交 `eb6b199` / `d1720ed` 给智联加了默认单轮上限：`AUTOMATION_24H_ZHILIAN_MAX_TOTAL` 未配置时默认 3。
  - 因此 24h 每轮只传 `maxTotal=3` 给智联，最多两轮就是每个账号 6 人；即使 `remainingActionable` 还有 25/32，也会结束该账号。
  - 本地已修改 `patchwork-recruit-gpt/server/services/automation24h.js`：智联不再默认限 3，只有显式配置 `AUTOMATION_24H_ZHILIAN_MAX_TOTAL>0` 时才限流，否则使用全局 `AUTOMATION_24H_MAX_TOTAL`。
- 验证：
  - `python -m py_compile agent_web_server.py` 通过。
  - `python -c "import agent_web_server; ..."` 通过，只有历史正则 warning。
  - `node --check patchwork-recruit-gpt/server/services/automation24h.js` 通过。
- 服务器状态：
  - 2026-06-15 13:00 左右的线上 24h 结果显示：智联宋峰峰处理 6 人后剩余 `remainingActionable=25`；智联和新红处理 6 人后剩余 `remainingActionable=32`，原因与默认 3 人上限吻合。
  - 服务器文件覆盖仍受远程权限限制；本地已生成 51 修复部署包 `tmp_deploy/job51_b_follow_song_20260615.zip`。
