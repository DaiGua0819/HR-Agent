# 2026-06-15 项目修改快照


## 2026-06-15 15:10 约面试联系人桥接与演示状态

### 已完成变更
- 将约面试入口从“简历字段直接发起”调整为“简历库记录 -> 消息处理数据库联系人记录”的桥接逻辑。
- 新增 `findResumeContactBridge()` / `applyResumeContactBridge()`，优先通过 `platformContact/sourceKey/conversationKey` 关联消息处理阶段保存的联系人上下文。
- 桥接评分规则：`>=110` 视为高置信可用，`<25` 视为不可确认。
- 成功桥接后会回填 `platformContact.displayName`、`sourceKey`、`conversationKey`、`candidateIdentityKey` 和少量 `chatEvidence`。
- 前端简历库点击“约面试”时，优先使用桥接后的平台联系人显示名去平台搜索。
- 已提交 `5b05de9 实现约面试联系人桥接`，并推送到 GitHub `main`。

### 本地验证结果
- 读取本地 `/api/resumes` 的 2026-06-15 简历记录后，发现部分记录缺少 `platformContact/sourceKey`。
- 使用平台、账号、候选人姓名、文件名和消息处理数据库进行桥接模拟。
- 当天可识别平台来源的 28 份简历中，高置信匹配 28 份，失败 0 份。
- 今天简历库第一位候选人为 `杨子毅`，来源 `BOSS 和新红`，模拟桥接到 `boss_b`，评分 `235`。

### 服务器部署状态
- GitHub 最新代码已经到 `5b05de9`。
- 服务器尚未完成部署，原因是当前可用远程通道都无法执行命令：
  - SSH 22 端口可达，但 `Administrator` 密码认证会被服务端关闭。
  - Paramiko 返回 `Authentication failed: transport shut down or saw EOF`。
  - WinRM 5985 端口可达，但 NTLM/Basic 都被 reset/disconnect。
  - SMB 访问 `\\218.244.142.84\C$` / `ADMIN$` 返回 `System error 64`。
  - WMI/RPC 返回 `RPC server is unavailable`。
  - CDP 端口 9222/9230/9224/9225/9226/9231 TCP 可达，但 HTTP `/json/version` 超时，不能作为部署通道。
- 当前服务器仍需要通过 RDP 登录后手动 `git pull` 并重启服务，或者提供一个可用的远程命令通道。

### 演示安全边界
- 约面试演示必须避免真实发送消息，除非用户在执行当时明确确认。
- 当前 agent 的 `dryRun=true` 只会打开平台、搜索、校验联系人、定位聊天输入框。
- 当前 `dryRun=true` 不会把 `加我微信沟通，carhhxh` 填进输入框。
- 非 `dryRun` 分支会填入并发送消息，不能直接用于“填好但不发送”的演示。
- 若要演示“输入消息但停止发送”，需要新增 `draftOnly` / `prepareOnly` 模式：搜索 -> 校验 -> 点开会话 -> 填入消息 -> 不点击发送。
- 演示结束后如该模式只是临时功能，需要按用户要求删除临时代码并提交记录。
- 演示前必须确认目标账号 agent ready。最近一次检查中 `boss_b` 浏览器/CDP ready，但 agent 未 ready，`agentError=fetch failed`，所以不能直接演示。

### 待办
- 如需演示“输入但不发送”，先实现 `draftOnly` / `prepareOnly` 模式。
- 部署前需恢复可用的服务器命令通道，或由用户 RDP 到服务器执行拉取和重启。
- 演示前再次确认 `boss_b` 或目标账号的浏览器、CDP、agent 都处于 ready。

## 长期文档更新约定
- 后续每次代码修改、部署尝试、服务器状态检查、线上问题排查、安全边界和遗留问题，都必须同步写入 `项目修改快照/YYYY-MM-DD-项目修改快照.md`。
- 记录至少包含：改了什么、为什么改、涉及哪些文件、验证结果、Git 提交状态、是否已部署到服务器、还有哪些未解决问题。
- 不能只在聊天里说明关键结论；需要把可接力的信息沉淀到 MD。

## 2026-06-15 15:20 线上约面试按钮不可点击排查

### 现象
- 用户反馈前端简历库里的“约面试”仍然点击不了。

### 排查结果
- 线上 `http://218.244.142.84:8080/src/client/scoring/rules.js` 仍然是旧版本：
  - 不包含 `resumeNeedsInterviewContactBridge()`。
  - 不包含 `getResumePlatformContactDisplayName()`。
  - 仍然包含旧判断：没有 `platformCandidateId` 时返回“历史简历缺少平台ID，无法按ID搜索约面试”。
- 线上 `server/legacy-parts/server.part004.js` 也仍然是旧版本：
  - 不包含 `findResumeContactBridge()`。
  - 不包含按 `platformContact.displayName` 生成 `searchName` 的逻辑。
  - `resolveResumeInterviewTarget()` 仍然要求 `platformCandidateId`。
- 线上 `/api/resumes` 当前返回 691 条简历，统计结果：
  - `platformCandidateId`：0 条。
  - `platformContact`：0 条。
  - `platformContact.displayName`：0 条。
  - `sourceKey`：0 条。
  - `interviewInvite.status=sent`：0 条。
  - `interviewInvite.status=failed`：0 条。

### 根因
- GitHub 已经有 `5b05de9 实现约面试联系人桥接` 和 `6f66fed 补充长期文档更新约定`，但服务器 Web 项目没有部署到最新版本。
- 线上仍运行旧版“按平台ID约面试”逻辑，而当前简历库没有任何 `platformCandidateId`，所以按钮被旧前端保护性禁用。

### 处理方式
- 需要先把服务器 `C:\RecruitAgent\project\patchwork-recruit-gpt` 更新到 GitHub 最新 `main`，并重启 `RecruitAgentWeb8765`。
- 更新后按钮逻辑会从“必须有平台ID”变为：
  - 自动化平台来源可点击。
  - 已有 `platformContact.displayName` 时显示“约面试”。
  - 缺少 `platformContact.displayName` 但来源可识别时显示“查找联系人”，点击后由后端桥接消息处理数据库。
- 注意：当前远程命令通道仍不可用，不能声称已部署；需要 RDP 到服务器执行 `git pull` 和重启，或恢复可用 SSH/WinRM/SMB/WMI 通道。

## 2026-06-15 15:30-15:40 线上约面试按钮修复与部署

### 修复内容
- 恢复 SSH 部署通道后，已将约面试桥接相关代码部署到服务器。
- 前端 `src/client/scoring/rules.js` 已从旧的“必须有 `platformCandidateId` 才能点击”改为：
  - 自动化平台来源可点击。
  - 缺少 `platformContact.displayName` 时按钮显示“查找联系人”。
  - 点击后由后端按单个候选人桥接消息处理数据库。
- 后端 `server.part004.js` 已使用 `findResumeContactBridge()` / `platformContact.displayName`，旧的 `missing_platform_candidate_id` 硬拦截已移除。
- 约面试接口新增 `ensureResumeInterviewAgentReady()`：点击约面试时按目标平台账号自动拉起对应 agent，避免因 agent 未手动启动直接 `fetch failed`。
- 关闭 `ensureDatabase()` 里的全库 `backfillResumeContactMetadata()` 启动回填，只保留约面试接口内单条按需桥接。

### 中间问题
- 第一次部署桥接代码后，`/api/resumes` 触发 Node OOM：
  - 日志显示 `FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory`。
  - 原因是启动/读库时全量联系人回填会扫描大量自动化聊天/日志数据。
  - 修复为：不在简历库列表加载或数据库初始化时做全库联系人桥接，只在点击某个候选人约面试时按需查找。

### 服务器部署记录
- 初次桥接部署备份：`C:\RecruitAgent\project\rollback_backups\interview_bridge_fix_20260615_153129`
- 按需桥接修复备份：`C:\RecruitAgent\project\rollback_backups\interview_bridge_on_demand_20260615_153637`
- 自动拉起 agent 修复备份：`C:\RecruitAgent\project\rollback_backups\interview_ensure_agent_20260615_153936`
- 最终覆盖文件：
  - `patchwork-recruit-gpt/src/client/scoring/rules.js`
  - `patchwork-recruit-gpt/server/legacy-parts/server.part000.js`
  - `patchwork-recruit-gpt/server/legacy-parts/server.part003.js`
  - `patchwork-recruit-gpt/server/legacy-parts/server.part004.js`
  - `agent_core/legacy_parts/agent_web_server.part003.py`
  - `agent_core/legacy_parts/agent_web_server.part004.py`
  - `agent_core/legacy_parts/agent_web_server.part018.py`
- 重启方式：停止 `\RecruitAgentWeb8765`，按 8765 监听端口杀旧 Web PID，再重新运行 `\RecruitAgentWeb8765`。

### 验证结果
- 服务器编译：
  - Node legacy server 拼接编译通过。
  - Python agent `py_compile agent_web_server.py` 通过。
- 公网验证：
  - `GET http://218.244.142.84:8080/index.html?...` 返回 200。
  - `GET /api/resumes` 返回 200，响应约 2.09MB。
  - 线上 `rules.js` 已包含 `resumeNeedsInterviewContactBridge` 和“查找联系人”，不再包含旧的 `platformCandidateId` 按钮硬拦截。
  - 线上 `server.part004.js` 已包含 `ensureResumeInterviewAgentReady`、`findResumeContactBridge` 和 `searchName: platformContact.displayName`，不再包含 `missing_platform_candidate_id`。
- 按线上 691 条简历模拟新前端按钮状态：
  - 688 条自动化平台来源可点击，当前会显示“查找联系人”。
  - 3 条非自动化来源仍会禁用。
  - 0 条已经标记为已约面试。
- 当前 24h 自动运转状态：`stopped`，本次修复没有启动 24h。

### 仍需注意
- 51 和新红账号当前页面处于登录页，后续点击该账号来源候选人的约面试时仍可能需要人工登录。
- BOSS/51 的 agent 当前不是常驻 ready，但约面试接口会在点击时尝试自动拉起对应 agent。

## 2026-06-15 15:45 简历库行距与操作按钮布局优化

### 修改内容
- 用户反馈添加约面试后，简历库每个联系人的行距又变宽，右侧按钮占用过大。
- 调整 `src/styles/resumes/actions.css`：
  - 右侧操作列从 `220px` 收窄到 `176px`。
  - 操作按钮改为固定 2 列网格：第一行“查看 / 查看聊天”，第二行“查找联系人或约面试 / 删除”。
  - 操作按钮高度从 30px 左右压到 26px，字号降到 11px。
- 调整 `src/styles/resumes/batch.css`：
  - 只针对简历库表格新增 `padding: 8px 9px`，批量任务表和规则表继续保留原通用 `13px 12px`。
  - 简历库表格行高固定为 1.35。
- 调整 `src/styles/scoring/responsive.css`：
  - 同步右侧操作列宽度为 `176px`，避免响应式样式把宽度撑回去。

### 部署与验证
- 已部署到服务器。
- 首次备份目录：`C:\RecruitAgent\project\rollback_backups\resume_table_compact_20260615_154554`。
- 最终作用域修正备份目录：`C:\RecruitAgent\project\rollback_backups\resume_table_compact_scope_fix_20260615_154717`。
- 覆盖文件：
  - `patchwork-recruit-gpt/src/styles/resumes/actions.css`
  - `patchwork-recruit-gpt/src/styles/resumes/batch.css`
  - `patchwork-recruit-gpt/src/styles/scoring/responsive.css`
- 该变更只覆盖静态 CSS 文件，未重启 24h，也未启动 agent。
- 公网验证：
  - `actions.css` 已包含 `grid-template-columns: repeat(2, 78px)` 和 `width: 176px`。
  - `batch.css` 已包含简历表格 `padding: 8px 9px`。
  - `batch.css` 通用表格 padding 仍保留 `13px 12px`，未继续影响批量任务表/规则表。
  - `responsive.css` 已包含操作列 `width: 176px`。
  - `GET /api/resumes` 返回 200。

## 约面试搜索依据调整

- 将简历库“约面试”能力从平台候选人 ID 搜索调整为平台联系人显示名搜索。
- 新增 `platformContact` 轻量元数据：
  - `displayName`：平台联系人当前使用的显示名，用于搜索。
  - `label`：联系人列表整行文本，用于人工排查。
  - `appliedPosition`：当时沟通岗位，用于同名联系人校验。
  - `chatEvidence`：最近 3 条有效聊天片段，用于辅助确认。
- 新简历下载记录会在 agent 侧保存 `platformContact`；Web 入库扫描会按 PDF 的 hash/path/filename 从下载索引回捞该字段并写入简历记录。
- 不再为新入库链路写入 `platformCandidateId` 或 `detailUrl`，历史字段仅保留兼容旧数据展示。

## 约面试执行保护

- 后端 `POST /api/resumes/:id/interview-invite` 改为要求 `platformContact.displayName`，缺失时按钮/接口均阻断。
- Agent `/api/interview-invite` 改为按 `searchName/platformContact.displayName` 搜索。
- 搜索后用联系人名、岗位、聊天证据和搜索结果数量做确认：
  - 同名多人且证据不足时返回 `multiple_candidates_unverified`。
  - 找不到可确认联系人时返回 `search_result_not_found`。
  - 岗位/聊天证据无法匹配时返回 `chat_evidence_not_matched`。
- 前端确认弹窗和按钮 tooltip 改为显示“平台联系人”，不再显示平台 ID。

## 文档

- 更新 `agent_skills/interview-scheduling/SKILL.md`，明确禁止用简历姓名、手机号、文件名、URL 或平台候选人 ID 兜底。
- 约面试固定话术保持为：`加我微信沟通，carhhxh`。

## 验证

- JS legacy server 分片拼接编译通过。
- Python agent legacy 分片拼接编译通过。
- 前端 `src/client/scoring/rules.js` 语法检查通过。

## 自动化统计日期与兜底修复

- 问题原因：
  - 24h 进度兜底会把 `target_completed/target_interrupted` 展开成候选人明细，且岗位固定写成“未识别岗位”，导致每日扇形图被兜底假记录污染。
  - 兜底进度只包含处理人数、求简历、获取简历，之前却被当成完整统计源使用，所以发话术、候选人提问、答疑等指标会被显示成不可靠的 0。
  - 日期逻辑允许“今天”读取前一天 36 小时内的 24h 日志；线上 `2026-06-15` 没有 24h 日志，却把 `2026-06-14` 的 53/21/9 处理数据算进了今天。
- 修复内容：
  - `automation24hTargetBelongsToDate` 改为严格按所选日期匹配，不再把前一天日志归到今天。
  - `automation24hLogDateKeys` 只读取当前选中日期的日志文件。
  - `/api/*-automation/details` 不再追加 `automation24hFallbackDetailRecords`，兜底只补充摘要 metrics，不生成候选人明细，也不参与岗位扇形图。
  - 每轮 24h 日志如果带有 `counts/resultMessage`，兜底摘要会尽量恢复发话术、求简历、获取简历、答疑等数字。
  - 前端每日扇形图额外过滤 `recoveredFrom24hStatus` 记录，作为防线。
- 服务器部署：
  - 备份目录：`C:\RecruitAgent\project\rollback_backups\stats_fallback_fix_20260615_102908`
  - 覆盖文件：`server.part001.js`、`server.part002.js`、`src/client/automation/overview.js`、`script.js`、`index.html`
  - 旧 8765 Node 子进程没有被 `schtasks /End` 杀掉，已按监听端口停止旧 PID，并通过 `RecruitAgentWeb8765` 拉起新 PID `19036`。
- 线上验证：
  - `GET /index.html?job=HRBP` 返回 200，脚本版本为 `20260615-stats-date-fix`。
  - `2026-06-15`：BOSS details 不再 recovered，明细 6 条、兜底假记录 0，岗位分布为 AI 应用开发实习生 5、人力资源管培生 1；51/智联今天没有 24h 兜底数据。
  - `2026-06-14`：24h 摘要仍可按昨天日期查看，且不再生成兜底假记录。

## 51 和新红误进人才管理、智联 24h 单轮 3 人排查

- 51 和新红问题：
  - 本地已修改 `agent_core/legacy_parts/agent_web_server.part002.py`、`part003.py`、`part005.py`。
  - 连接 51 CDP 时优先选择 `Revision/chat` 聊天页，并关闭误打开的 `Revision/talent/management` 页。
  - 51 打开聊天页时复用已有聊天页并 bring to front，避免留在人才管理页。
  - 51 点击联系人时优先点 `.username.at` 候选人姓名区域，找不到姓名时点列表左侧安全区，不再点整行中间，降低误触人才管理/简历入口概率。
- 智联只处理前几个人的原因：
  - 不是今天修 51 代码引起的；智联 agent 滚动函数本身没有被本次改动触碰。
  - 24h 调度里历史提交 `eb6b199` / `d1720ed` 给智联加了默认单轮上限：`AUTOMATION_24H_ZHILIAN_MAX_TOTAL` 未配置时默认 3。
  - 因此 24h 每轮只传 `maxTotal=3` 给智联，最多两轮就是每个账号 6 人；即使 `remainingActionable` 还有 25/32，也会结束该账号。
  - 本地已修改 `patchwork-recruit-gpt/server/services/automation24h.js`：智联不再默认限 3，只有显式配置 `AUTOMATION_24H_ZHILIAN_MAX_TOTAL>0` 时才限流，否则使用全局 `AUTOMATION_24H_MAX_TOTAL`。
- 验证：
  - `python -m py_compile agent_web_server.py` 通过。
  - `python -c "import agent_web_server; ..."` 通过，只有历史正则 warning。
  - `node --check patchwork-recruit-gpt/server/services/automation24h.js` 通过。
- 服务器状态：
  - 2026-06-15 13:00 左右的线上 24h 结果显示：智联宋峰峰处理 6 人后剩余 `remainingActionable=25`；智联和新红处理 6 人后剩余 `remainingActionable=32`，原因与默认 3 人上限吻合。
  - 服务器文件覆盖仍受远程权限限制；本地已生成 51 修复部署包 `tmp_deploy/job51_b_follow_song_20260615.zip`。

## 2026-06-15 16:10 周子杰约面试联系人桥接误判修复

### 问题原因
- 周子杰简历点击“约面试”失败，后端返回 `bridge_ambiguous_match`，提示“找到多个相似平台联系人”。
- 服务器诊断发现 BOSS 和新红账号的自动化日志里，同一个周子杰在不同处理阶段被记录成了多个不同 `conversationKey`：`chat_bbc9fa07ea91b73744e5`、`chat_dde9c49784f74680a0d6`、`chat_dbce298aff2cd3f9d743`。
- 这些记录姓名和岗位完全一致，但旧 bridge 只按 `conversationKey` 做身份区分，所以把同一个候选人的历史片段误判成多个相似联系人。

### 修复内容
- 修改 `server/legacy-parts/server.part000.js`：新增约面试 bridge 候选去重逻辑，优先按 `sourceKey + 平台显示名 + 沟通岗位` 合并同一候选人的多段日志。
- 保留 `conversationKey/candidateIdentityKey` 作为后备稳定身份，并排除“全部职位/未读/职位/候选人”等列表容器名称，避免把列表页误当成候选人。
- 歧义判断改为基于去重后的候选人列表，只有真正不同候选人同时高分时才阻断。
- 修改 `server/legacy-parts/server.part005.js`：新增 `requestBaseUrl()`，当请求缺少 `Host` 头时 fallback 到 `HOST:PORT`，避免探测请求触发 `TypeError: Invalid URL` 导致 8765 Web 进程崩溃。

### 部署与验证
- 已部署到服务器 `C:\RecruitAgent\project\patchwork-recruit-gpt`。
- 备份目录：`interview_bridge_dedupe_20260615_155716`、`interview_bridge_evidence_20260615_160117`、`interview_bridge_hostfix_20260615_160457`、`interview_bridge_final_20260615_161022`。
- 远程 legacy server 拼接编译通过。
- 8765 Web 后端已重启，新进程：`node.exe run_server_keepalive.js`，启动时间 2026-06-15 16:10。
- 临时诊断结果：周子杰 12 条相关日志已合并为一个 bridge 候选 `boss_b|display|周子杰|ai应用开发实习生`，bridge 返回 `ok:true`，最终匹配 `conversationKey=chat_dbce298aff2cd3f9d743`，`score=235`。
- dry-run 验证：已不再返回 `bridge_ambiguous_match`；后端已把周子杰简历补齐 `sourceKey=boss_b`、`conversationKey=chat_dbce298aff2cd3f9d743`、`platformContact.displayName=周子杰`。
- 当前新的阻塞点变为 BOSS 页面 `search_input_not_found`：bridge 已通过，但约面试 agent 打开 BOSS 搜索页后没有找到搜索输入框；这是后续页面定位问题，不是“多个相似联系人”问题。

## 2026-06-15 16:30 BOSS 约面试联系人搜索入口修复

### 问题原因
- BOSS 的联系人搜索入口不是左侧主导航“搜索”，而是联系人列表上方、`全部职位` 右侧的放大镜按钮。
- 原有 `open_interview_search_surface()` 只按可见文本“搜索/search”评分，导致误点左侧主菜单 `menu-geeksearch`，页面跳到 `https://www.zhipin.com/web/chat/search` 后自然找不到联系人搜索输入框，返回 `search_input_not_found`。
- 服务器 DOM 验证确认真实入口为 `.chat-search-btn`，位于 `.chat-job-search` 容器内，坐标约 `x=523,y=124,w=34,h=34`；点击后出现 `input.search-input`，placeholder 为“搜索姓名/群聊”，坐标约 `x=218,y=124,w=339,h=34`。

### 修复内容
- 修改 `agent_core/legacy_parts/agent_web_server.part018.py`：
  - BOSS 平台优先查找并点击 `.chat-job-search .chat-search-btn` / `.chat-top-filter .chat-search-btn` / `.chat-user .chat-search-btn`。
  - 对 BOSS 搜索入口增加联系人列表区域坐标限制：`x>=190`、`y=80~190`，并排除 `.side-wrap`、`.menu-list`、`.menu-geeksearch`，避免再点左侧导航。
  - 搜索输入框识别补充父级 class：`search-input`、`chat-top-search`、`chat-job-search`、`conversation-search`，确保 BOSS 顶部搜索框被优先识别。
  - 搜索类输入框会从聊天输入框候选里降权，避免搜索失败时误把左侧搜索框当成右侧聊天编辑器。

### 部署与验证
- 已部署到服务器 `C:\RecruitAgent\project\agent_core\legacy_parts\agent_web_server.part018.py`。
- 服务器备份：`C:\RecruitAgent\project\rollback_backups\agent_web_server.part018.before_boss_interview_search_20260615_162930.py`、`C:\RecruitAgent\project\rollback_backups\agent_web_server.part018.before_boss_interview_search_final_20260615_163258.py`。
- 远程 agent 分片拼接编译通过；只重启了 8788 BOSS 和新红 agent，浏览器/CDP 9230 未关闭。
- dry-run 验证周子杰约面试链路通过：`ok=true`、`platform=boss`、`contactVerified=true`、`inputReady=true`、`dryRun=true`。
- 第二次 dry-run 验证到新入口：`openedSearch.strategy=boss_contact_search_icon`、`openedSearch.className=chat-search-btn`、`openedSearch.rect=x=523,y=124,w=34,h=34`，确认不再点击左侧菜单。
- 最终 dry-run 复核：`beforeFirstSearchLabel=search-input/search-box/chat-top-search/chat-job-search`，`beforeFirstChatLabel=boss-chat-editor-input/conversation-editor`，确认搜索框和聊天输入框已分离识别。

## 2026-06-15 17:20 51/智联约面试搜索入口与 functioncall 修复

### 真实页面定位
- 51job 两个账号真实入口一致：人才沟通页屏幕右上方 `.conversation-search .search-box input[placeholder*="搜索姓名"]`。
  - 51 A：输入框约 `x=1387,y=136,w=272,h=28`。
  - 51 B：输入框约 `x=1573,y=135,w=272,h=28`。
  - 不能点击左侧 `人才管理` / `人才搜索` 导航。
- 智联两个账号入口与 BOSS 类似：左侧联系人列表头部 `.side-panel-header__input-button`。
  - 智联 A：搜索按钮约 `x=404,y=74,w=32,h=32`。
  - 智联 B：搜索按钮约 `x=395,y=74,w=32,h=32`。
  - 点击后出现 `.im-search-modal input[placeholder*="搜索聊天记录/姓名/职位/公司"]`，再输入平台联系人显示名。

### 修复内容
- 修改 `agent_core/legacy_parts/agent_web_server.part018.py`：
  - `open_interview_search_surface()` 增加 51job 和智联平台专用搜索入口定位。
  - `interview_page_probe()` 增加父级/祖父级 class 参与输入框识别，补充 `conversation-search/search-box/im-search-modal/side-panel-header__input` 等搜索特征。
  - 搜索结果按平台候选行优先排序：51 优先 `.list-item/.conversation-item/.wrap-item`，智联优先 `.im-search-result`，避免点到外层页面容器或搜索弹窗容器。
  - 点击结果时会落到最近的候选行容器，避免只点姓名 span。
  - 智联点击搜索结果后增加输入框等待重试，解决结果已打开单聊但聊天面板加载稍慢导致 `chat_input_not_found` 的问题。
  - dry-run 如果没有可点击结果且没有聊天输入框，返回 `search_result_not_found`，不再误报已确认联系人。
- 修改 `patchwork-recruit-gpt/server/legacy-parts/server.part001.js` 和 `start_recruit_agent_vm.ps1`：
  - 启动 agent 前清理 `PYTHONHOME/PYTHONPATH`，避免 Python 3.12 混入 LibreOffice Python 3.11 标准库。
- 更新 `agent_skills/interview-scheduling/SKILL.md`：
  - 明确 BOSS、51job、智联三平台搜索入口。
  - 保持约面试必须使用 `platformContact.displayName`，不能用简历姓名、手机号、URL 或平台 ID 兜底。

### 部署与验证
- 已部署到服务器：
  - `C:\RecruitAgent\project\agent_core\legacy_parts\agent_web_server.part018.py`
  - `C:\RecruitAgent\project\patchwork-recruit-gpt\server\legacy-parts\server.part001.js`
  - `C:\RecruitAgent\project\start_recruit_agent_vm.ps1`
  - `C:\RecruitAgent\project\agent_skills\interview-scheduling\SKILL.md`
- 远程 Python agent 分片拼接编译通过；远程 legacy server 拼接语法检查通过。
- 8765 Web 已重启，`/index.html` 返回 200。
- 用 `Win32_Process.Create` 重启 8789/8790/8791/8792，四个 agent 均返回 `/api/status=200`；临时 8899 测试 agent 已关闭。
- dry-run 验证：
  - 51 B / 郭莹：`ok=true`，点击 `.list-item selected`，已定位聊天输入框 `发送给 郭莹`。
  - 智联 B / 李晓磊：`ok=true`，点击 `.im-search-result`，已定位底部输入框 `从这里开启对话...`。
  - 智联 A / 魏静：已验证能通过 `.side-panel-header__input-button` 打开搜索并定位输入框。
  - 51 A / 郑子璇：当前搜索条件下没有可点击结果，返回 `search_result_not_found`，属于安全阻断，不会误发。

## 2026-06-15 19:10 51 约面试搜索结果点击修复

### 问题原因
- 51job 搜索联系人后，真实结果行位于 `.search-content .name-res.result .searchlist-item .conversation-item`。
- 搜索结果文本通常就是候选人姓名，例如“郭莹”，旧逻辑把“文本完全等于搜索词”的节点当成搜索框回显过滤掉。
- 同时旧逻辑会对 `.conversation-search/.search-box` 内的节点强降权，导致“搜到了联系人但没有点击联系人”，最终停在原会话。

### 修复内容
- 只修改 `agent_core/legacy_parts/agent_web_server.part018.py` 的 51 约面试搜索识别：
  - 新增 51 搜索结果行识别，允许 `.search-content/.searchlist-item/.name-res.result/.conversation-item` 通过搜索回显过滤。
  - 51 搜索结果行获得高优先级评分，不再被 `.search-box` 降权。
  - 点击结果时优先落到 `.search-content .searchlist-item` 或 `.search-content .conversation-item`。
- 保留约面试测试保护：当前仍不会填写消息，也不会点击发送。

### 部署与验证
- 已备份服务器文件：`C:\RecruitAgent\project\rollback_backups\agent_web_server.part018.before_51_interview_click_20260615_190833.py`。
- 已部署到服务器并重启 8789/8791 两个 51 agent。
- 本地 legacy agent 分片拼接编译通过。
- 线上验证：
  - 51 B / 郭莹：已识别 `.name-res result` 搜索结果并点击，最终输入框为 `发送给 郭莹`，返回 `interview_send_disabled_for_testing`。
  - 51 A / 郑子璇：已识别 `.name-res result` 搜索结果并点击，最终输入框为 `发送给 郑子璇`，返回 `interview_send_disabled_for_testing`。

## 2026-06-15 19:35 约面试 100 人 dry-run 深度测试

### 测试边界
- 使用前端同一路径 `POST /api/resumes/:id/interview-invite`，请求参数为 `dryRun:true, force:true`。
- 只测试桥接、搜索、点击联系人、校验输入框，不填写约面试话术，不点击发送。
- 测试前备份服务器简历库数据库，测试后恢复备份并重启 8765，避免 100 条 dry-run 状态污染前端简历库。
- 结果文件保存在服务器：`C:\RecruitAgent\project\logs\interview_invite_deep_test_20260615_192632.result.json`。

### 样本分配
- 简历库共 694 条；可归属到 6 个平台账号的记录数：
  - `boss_a`: 242
  - `boss_b`: 292
  - `job51_a`: 36
  - `job51_b`: 4
  - `zhilian_a`: 79
  - `zhilian_b`: 23
- 因 `job51_b` 只有 4 条，无法做到 6 个号完全均分 100 条；最终按尽量均衡分配：
  - `boss_a`: 19
  - `boss_b`: 20
  - `job51_a`: 19
  - `job51_b`: 4
  - `zhilian_a`: 19
  - `zhilian_b`: 19

### 测试结果
- 总计 100 条 dry-run，0 条真实发送。
- 通过 71 条，失败 29 条。
- 通过标准：前端 dry-run 成功、agent `contactVerified=true`、`inputReady=true`。
- 94 条的简历库姓名与搜索名一致；6 条因桥接失败或异常没有拿到搜索名。
- 分平台账号：
  - BOSS 宋峰峰：2/19 通过；主要失败为 `search_input_not_found` 15 条。
  - BOSS 和新红：17/20 通过；失败为 `search_result_not_found` 2 条、`bridge_ambiguous_match` 1 条。
  - 51 宋峰峰：18/19 通过；失败为 `search_result_not_found` 1 条。
  - 51 和新红：0/4 通过；4 条均为 `search_result_not_found`，平台搜索没有返回可确认联系人。
  - 智联 宋峰峰：16/19 通过；失败为 `search_result_not_found` 1 条、`bridge_no_high_confidence_match` 2 条。
  - 智联 和新红：18/19 通过；失败为 `bridge_no_high_confidence_match` 1 条。

### 结论
- 51 宋峰峰账号的约面试搜索点击链路基本正常，刚修复的“搜到结果但不点击联系人”问题未复现。
- 51 和新红账号不是点击失败，而是样本太少且 4 条搜索均未返回可确认联系人；需要确认这些历史简历在该账号当前会话列表/搜索中是否仍可查。
- BOSS 宋峰峰存在明显异常：前两条可通过，随后出现一次页面导航导致 `Execution context was destroyed`，之后连续 `search_input_not_found`，应单独修复 BOSS A 搜索入口恢复/重开逻辑。
- BOSS 和新红、智联两个账号整体可用；剩余失败主要来自桥接证据不足或搜索无结果，属于安全阻断，没有误点发送。

### BOSS 宋峰峰重跑验证
- 初次 100 人测试后，CDP 检查发现 BOSS 宋峰峰浏览器曾停在 `安全验证 - BOSS直聘`，URL 为 `passport/zp/verify.html`，而 BOSS 和新红正常停在 `web/chat/index`。
- 用户处理后再次检查，BOSS 宋峰峰已回到 `web/chat/index?_security_check=...`。
- 单独重跑 BOSS 宋峰峰 19 条 dry-run：
  - 通过 16 条，失败 3 条，真实发送 0 条。
  - 不再出现 `search_input_not_found`。
  - 失败明细：
    - 况轲舰：`search_result_not_found`，搜索后没有可确认联系人。
    - 杨譞：`bridge_ambiguous_match`，找到多个相似联系人，安全阻断。
    - 范玉康：`search_result_not_found`，搜索后没有可确认联系人。
- 结论：上一轮 BOSS 宋峰峰 15 条 `search_input_not_found` 主要由安全验证/页面跳转状态导致，不是 BOSS 宋峰峰和 BOSS 和新红的约面试代码逻辑不同。

## 2026-06-15 19:45 恢复约面试真实发送

- 将 `agent_core/legacy_parts/agent_web_server.part018.py` 中 `INTERVIEW_INVITE_SEND_DISABLED_FOR_TESTING` 的默认值从 `"1"` 改为 `"0"`。
- 保留环境变量开关：后续如果需要临时测试，可设置 `INTERVIEW_INVITE_SEND_DISABLED_FOR_TESTING=1` 重新禁用填写和发送。
- 已部署到服务器并重启 6 个 agent：
  - 8787 BOSS 宋峰峰
  - 8788 BOSS 和新红
  - 8789 51 宋峰峰
  - 8790 智联 宋峰峰
  - 8791 51 和新红
  - 8792 智联 和新红
- 服务器备份文件：`C:\RecruitAgent\project\rollback_backups\agent_web_server.part018.before_restore_interview_send_20260615_194524.py`。
- 线上验证：
  - 远程 `part018.py` 默认值已是 `"0"`。
  - 启动脚本未设置 `INTERVIEW_INVITE_SEND_DISABLED_FOR_TESTING`。
  - 6 个 agent `/api/status` 均返回 200。
- 当前状态：从前端点击“约面试”将恢复真实填写固定话术并点击发送。

## 2026-06-15 20:10 约面试改为点击平台“换微信”

### 真实页面定位
- BOSS：聊天底部右侧 `.conversation-operate .operate-exchange-left`，按钮为 `.operate-icon-item` / `.operate-btn`；未交换时执行“换微信/交换微信”，已交换时显示“查看微信”。
- 51job：人才沟通页右侧候选人操作区 `.custom-operate` 内的 `.exchange-wx-btn` / `.exchange-wx-wrap`，文字为“换微信”。
- 智联：聊天底部 `.im-session-detail-footer .session-new-action` 内的 `.im-ask-for-wx`，按钮文字为“换微信”；已交换时可能显示“微信号/复制微信号”。

### 修改内容
- `agent_core/legacy_parts/agent_web_server.part018.py`：
  - 新增换微信按钮探测、确认弹窗探测、点击后状态校验。
  - `/api/interview-invite` 非 `dryRun` 分支不再填写或发送固定文本，改为联系人校验通过后点击平台内置“换微信”。
  - `dryRun=true` 只搜索、核对、定位换微信按钮，不点击换微信。
  - 新增失败原因：`wechat_exchange_button_not_found`、`wechat_exchange_button_disabled`、`wechat_exchange_confirm_failed`、`wechat_exchange_verification_failed`。
- `patchwork-recruit-gpt/server/legacy-parts/server.part004.js`：
  - 下发 `action: "exchange_wechat"`，不再下发固定消息。
  - 成功默认文案改为“已发起换微信”。
- `patchwork-recruit-gpt/src/client/scoring/rules.js`：
  - 前端确认弹窗改为“发起换微信”，不再展示“发送内容：加我微信沟通，carhhxh”。
  - 按钮 tooltip 改为“按平台联系人名搜索并执行：点击平台内置换微信”。
- `agent_skills/interview-scheduling/SKILL.md`：
  - 更新约面试 skill 定义，明确约面试动作不再发送固定文本，而是点击平台“换微信”。

### 验证
- 本地 agent legacy 分片拼接编译通过。
- 本地 Web legacy server 分片拼接语法检查通过。
- 本地 `patchwork-recruit-gpt/src/client/scoring/rules.js` 语法检查通过。

## 2026-06-15 20:30 约面试换微信线上验证

### 线上状态
- 服务器：`218.244.142.84`，项目路径 `C:\RecruitAgent\project`。
- 8765 Web 服务正常，公网 `http://218.244.142.84:8080/index.html?job=HRBP` 返回 200。
- 6 个浏览器和 6 个 agent 均 ready：
  - 8787 BOSS 宋峰峰
  - 8788 BOSS 和新红
  - 8789 51 宋峰峰
  - 8790 智联 宋峰峰
  - 8791 51 和新红
  - 8792 智联 和新红
- 前端线上 `src/client/scoring/rules.js` 已包含“点击平台内置换微信”文案。

### dry-run 验证
- 使用服务器本机 agent 接口 `/api/interview-invite/probe`，只搜索、核对、定位换微信，不点击真实换微信。
- BOSS 和新红，搜索 `郑晓妍`：
  - `ok=true`
  - `contactVerified=true`
  - `wechatExchangeReady=true`
  - 页面状态为已交换，按钮文案为“查看微信”。
- 51 宋峰峰，搜索 `魏迪`：
  - `ok=true`
  - `contactVerified=true`
  - `wechatExchangeReady=true`
  - 页面状态为可发起“换微信”。
- 智联 宋峰峰，搜索 `黄智豪`：
  - `ok=true`
  - `contactVerified=true`
  - `wechatExchangeReady=true`
  - 页面状态为已交换，按钮文案为“微信号”。

### 结论
- 三个平台的搜索、联系人核对、换微信入口定位链路均可用。
- 当前非 dry-run 的真实约面试动作已经从发送固定文本切换为点击平台内置“换微信”。
- 如后续要临时测试到按钮前停止，必须使用 `dryRun=true`。

## 2026-06-15 20:45 今日简历库数量差异排查

### 用户问题
- 前端今日简历库只有 32 份。
- 各平台统计合计看起来有 50 多份简历。
- 需要判断差异来自下载失败、导入失败、重复简历，还是统计口径问题。

### 对账范围
- 服务器数据库：`C:\RecruitAgent\project\patchwork-recruit-gpt\data\resumes.sqlite`
- 今日统计范围：北京时间 2026-06-15 00:00 至排查时刻。
- 对账对象：
  - SQLite `resumes`
  - SQLite `batch_jobs` / `batch_items`
  - `job51_import_manifest.json`
  - `zhilian_import_manifest.json`
  - `job51_resume_downloads*.json`
  - `recruiter_resume_downloads*.json`
  - `agent_files.zhilian_*.json`
  - `automation-24h-logs/2026-06-15.jsonl`
  - `patchwork-recruit-gpt/boss-resumes`
  - `patchwork-recruit-gpt/data/uploads`
  - `patchwork-recruit-gpt/data/batch_uploads`

### 核心结果
- 今日 `createdAt` 新增入库：32 份。
  - BOSS/邮箱：22
  - 51job：2
  - 智联：8
- 今日 `updatedAt` 变更记录：44 份。
  - 其中 12 份是历史简历今天被更新来源、约面试桥接或重复导入状态，不属于今日新增。
- 今日导入队列 `batch_items`：39 条。
  - `saved`：32
  - `duplicate`：7
  - `failed`：0
- 今日原始下载或接收的唯一 PDF 约 51 份。
  - BOSS/邮箱：24
  - 51job：3
  - 智联：24

### 差异原因
- 统计口径重复：
  - 51job 下载流水今天有重复记账。
  - 陈铭杰、胡红锋、孙文等同一文件在下载 JSON 中各出现多次。
  - 51job 今日真实唯一文件是 3 份，不应按下载流水条数直接相加。
- 重复简历不计入今日新增：
  - 今日有 7 份被判定为已上传过，不进入前端“今日新增”。
  - 示例：曾文毅、张力鑫、王静柯、毛子鸣、王关键、魏迪、吴云鹏。
- 智联存在导入清单和数据库不一致：
  - 一批智联 manifest 记录显示 `saved`，但当前 SQLite 的 `batch_jobs`、`batch_items`、`resumes` 对应行不存在。
  - PDF 文件仍在 `data/uploads`，说明文件曾经解析或复制成功，但数据库记录后续丢失或被覆盖。
  - 典型缺失记录：曾少宾、曾祥跃、江先生、李梦阳、皮俊平、魏涛、吴佳乐、杨辰。
- 智联还有下载流水有文件但没有进入导入清单的记录：
  - 玛丽金
  - 林翌健
  - 陈吉春

### 初步判断
- 32 份不是前端显示错，而是 SQLite 当前真实新增入库数。
- 50 多份是“平台下载/接收统计”的粗口径，包含重复流水、重复简历、导入清单异常和未入队文件。
- 最大风险点是智联导入链路：manifest 可能先标记为 `saved`，但数据库行后续不存在，导致后续扫描认为已导入而跳过。

### 后续修复建议
- 增加 manifest 与 SQLite 的一致性校验：
  - manifest 标记 `saved` 时必须确认 `resumeId` 在 `resumes` 表中存在。
  - 如果 `resumeId` 不存在，应把该 manifest 条目回滚为待导入或重新排队。
- 导入扫描时不要只信任 manifest 的 `saved/duplicate` 状态，需二次校验 `batch_items` 和 `resumes`。
- 平台统计中的“获取简历”应按唯一文件或唯一候选人去重，不应按下载流水事件条数相加。
- 对今天缺失的智联 PDF 可从 `data/uploads` 或原下载目录重新入库，避免这批简历丢失在前端简历库之外。

## 2026-06-15 20:55 工作记录约束

- 后续每次执行排查、修复、部署、验证任务，都需要同步更新当天项目修改快照 MD。
- MD 里至少记录：
  - 用户问题
  - 排查范围
  - 根因判断
  - 修改文件
  - 部署位置
  - 验证结果
  - 未完成风险和下一步建议
- 如果只是查询服务器状态，也需要记录关键结论，避免后续会话丢失上下文。
