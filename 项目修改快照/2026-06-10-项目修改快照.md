# 2026-06-10 项目快照

## 一句话状态

- 今天主要围绕服务器恢复、六个启动按钮、实时状态、主动联系岗位分离、简历入库稳定性做了修改和部署。
- 服务器当前 Web 服务已恢复：`http://127.0.0.1:8765/index.html` 返回 `200`。
- 当前本地还有未提交改动：`patchwork-recruit-gpt/server/legacy-parts/server.part003.js`、`patchwork-recruit-gpt/server/legacy-parts/server.part004.js`，这两个文件已经部署到服务器，用于简历入库稳定性修复。

## 项目架构速览

- `patchwork-recruit-gpt/`：前端页面、Node Web 服务、简历库、自动化状态接口。
- `patchwork-recruit-gpt/index.html`：主页面。
- `patchwork-recruit-gpt/styles.css`：总样式入口，引用拆分后的 CSS。
- `patchwork-recruit-gpt/src/client/core/init.js`：前端初始化、按钮事件、状态轮询等。
- `patchwork-recruit-gpt/src/client/core/state.js`：前端 DOM 引用和状态变量。
- `patchwork-recruit-gpt/src/styles/base/layout.css`：启动按钮和布局样式。
- `patchwork-recruit-gpt/server/legacy-parts/`：Node 后端 legacy 分片，启动时拼接加载。
- `patchwork-recruit-gpt/server/legacy-parts/server.part001.js`：自动化状态、浏览器/agent 状态、按钮状态后端支持。
- `patchwork-recruit-gpt/server/legacy-parts/server.part003.js`：简历解析、批处理 worker、批量入库执行。
- `patchwork-recruit-gpt/server/legacy-parts/server.part004.js`：文件夹扫描、自动入库、导入 manifest、邮箱自动导入。
- `agent_core/legacy_parts/`：Python agent 拆分文件，包含三平台自动化逻辑。
- `boss_chat_rules.json`：知识库/话术规则。

## 服务器信息

- 服务器：`218.244.142.84`
- 服务器项目目录：`C:\RecruitAgent\project`
- Web 目录：`C:\RecruitAgent\project\patchwork-recruit-gpt`
- 内部服务：`127.0.0.1:8765`
- 公网代理：`218.244.142.84:8080`
- Web 计划任务：`\RecruitAgentWeb8765`
- 8080 代理计划任务：`\RecruitAgentProxy8080`
- 部署原则：先备份到 `C:\RecruitAgent\project\rollback_backups\...`，再覆盖文件，再重启计划任务。
- 注意：不要在文档记录明文密码、API Key、Token。

## 今天 Git 提交

| 提交 | 时间 | 名称 | 主要内容 |
| --- | --- | --- | --- |
| `78b0593` | 16:27 | `服务器1.0` | 保存服务器 1.0 基线，涉及前端入口、脚本、按钮相关文件。 |
| `4a37c55` | 17:02 | `当前现场备份-20260610` | 保存当时现场，涉及 agent、前端、server、VM 工具等文件。 |
| `3c452f4` | 18:28 | `修复简历入库` | 修复简历入库自动检测/manifest/批处理相关逻辑。 |
| `26a063a` | 18:55 | `修复六个启动按钮` | 恢复六个按钮，每个按钮对应账号/平台启动逻辑。 |
| `52fe4ac` | 19:08 | `实时更新状态` | 将 CDP/agent 运行状态同步到“开始处理”按钮。 |
| `80c86dd` | 19:33 | `分离HRBP与人力资源管培生的主动联系` | 三个平台主动联系逻辑中区分 HRBP 和人力资源管培生。 |
| `8c606c3` | 20:16 | `优化启动按钮格式` | 启动按钮尺寸、字体、状态颜色、隐藏入口等 UI 调整。 |

## 今天早上做过的事

- 排查 `8765` Web 服务启动不稳定问题，测试 direct、wrapper、keepalive 启动方式。
- 生成 `patchwork-recruit-gpt/run_server_keepalive.js`，用于让 `node server.js` 退出后自动重启。
- 确认 `127.0.0.1:8765` 可以正常返回页面。
- 确认智联、51 自动简历入库扫描启动：每 `180` 秒扫描一次。
- 系统启动时补过简历来源字段和岗位匹配分。
- 排查出 51 的 4 条旧失败任务：毛子鸣、王关键、魏迪、吴云鹏，原因是旧本地路径残留，不是当天新下载失败。
- 上午服务日志显示部分 BOSS 邮箱附件和智联简历被正常解析/入库。

## 今天下午到晚上做过的事

### 版本和回退

- 保存 `服务器1.0` Git 提交。
- 保存 `当前现场备份-20260610` Git 提交。
- 查过 Git 历史，并围绕 `服务器1.0`、`11.02` 样式、六按钮布局做过前端恢复。
- 服务器备份目录出现过：`C:\RecruitAgent\project\rollback_backups\before_server1_0_20260610_174400`

### 六个启动按钮

- 恢复六个按钮，而不是一个“一键启动”按钮。
- 每个按钮对应账号和平台，点击后启动对应浏览器和 agent。
- 点击已启动按钮应走关闭流程：确认后关闭对应浏览器和 agent。
- 处理状态需要和 CDP/agent 实时同步。
- 颜色规则已改：
  - 已登录/可启动：蓝色。
  - 处理中：绿色。
  - 需要登录：黄色。
  - 账号异常：红色。
  - 人机验证：红色慢闪。
- 按钮尺寸统一为小尺寸，字体变小。
- 相关文件：
  - `patchwork-recruit-gpt/index.html`
  - `patchwork-recruit-gpt/styles.css`
  - `patchwork-recruit-gpt/src/styles/base/layout.css`
  - `patchwork-recruit-gpt/src/client/core/init.js`
  - `patchwork-recruit-gpt/server/legacy-parts/server.part001.js`

### 实时状态

- 将 CDP 和 agent 的状态检测同步到“开始处理”按钮。
- 之前用户要求 3 秒检测一次账号状态和进程状态。
- 已提交：`52fe4ac 实时更新状态`

### 主动联系岗位分离

- 三个平台主动联系模块中，`HRBP` 只能匹配 `HRBP`。
- `人力资源管培生` 只能匹配 `人力资源管培生`。
- 不修改知识库，只修改主动联系模块。
- 已提交：`80c86dd 分离HRBP与人力资源管培生的主动联系`
- 相关文件：
  - `agent_core/legacy_parts/agent_web_server.part005.py`
  - `agent_core/legacy_parts/agent_web_server.part007.py`
  - `agent_core/legacy_parts/agent_web_server.part008.py`
  - `agent_core/legacy_parts/agent_web_server.part010.py`
  - `agent_core/legacy_parts/agent_web_server.part013.py`
  - `agent_core/legacy_parts/agent_web_server.part014.py`

### 隐藏前端入口

- 隐藏了前端部分导入卡片：
  - `BOSS 简历文件夹`
  - `智联简历文件夹`
  - `保存邮箱简历到本地`
- 目的：减少前端误操作入口。
- 相关文件：
  - `patchwork-recruit-gpt/index.html`
  - `patchwork-recruit-gpt/styles.css`

## 晚上简历入库稳定性补丁

### 问题

- 今天发现收到/下载简历数与简历库入库数不一致。
- 核对后确认：收到/下载数包含重复、旧简历再次下载、失败重试；简历库只显示成功入库候选人。
- 查到 6 条失败：
  - 4 条 51 旧任务：毛子鸣、王关键、魏迪、吴云鹏，实际已在简历库。
  - 2 条 BOSS 旧任务：李女士、马丛超，旧任务路径还是本地 `C:\Users\24471\...`，服务器找不到。
- `88` 个 BOSS 待入库文件是 `2026-06-03` 的历史邮箱附件，不处理。

### 修复内容

- `patchwork-recruit-gpt/server/legacy-parts/server.part003.js`
  - 新增 `updateBatchItemFileReference(...)`。
  - 批处理 worker 读取 PDF 前先调用 `resolveBatchItemFileReference(...)` 找回真实文件。
  - 找不到真实文件时才失败。
  - 找回后更新 `file_path` 和 `payload.sourcePath`。

- `patchwork-recruit-gpt/server/legacy-parts/server.part004.js`
  - 新增同步扫描当前文件夹的兜底：
    - `listFolderResumePdfFilesSync(...)`
    - `fileHashMatches(...)`
    - `resolveBatchItemFileReference(...)`
  - `resumeInterruptedFolderImportJobs(...)` 恢复卡住任务时先按文件名/哈希找真实路径。
  - `handleRetryBatchJob(...)` 重试失败项时不再清空 `payload`，避免丢失来源、账号、哈希。
  - `markFolderImportCompleted(...)` 写 manifest 时使用当前真实路径。

### 部署状态

- 已部署到服务器。
- 服务器备份：`C:\RecruitAgent\project\rollback_backups\resume_import_stability_20260610_203739`
- 已重启：`\RecruitAgentWeb8765`
- 已验证：
  - `http://127.0.0.1:8765/index.html` 返回 `200`
  - `/api/resumes` 返回 `596`
  - 服务器拼接 legacy 代码语法通过
- 注意：该补丁已部署，但本地尚未 Git 提交。

## 今天新增内容

- 新增项目修改快照目录：`项目修改快照`
- 新增说明文档：`项目修改快照/2026-06-10-项目修改快照说明.md`
- 新增模板：`项目修改快照/2026-06-10-项目修改快照模板.md`
- 新增今天快照：`项目修改快照/2026-06-10-项目修改快照.md`

## 今天删除内容

- 未主动删除业务代码。
- 未删除简历、日志、备份。
- 未处理 `2026-06-03` 的 88 个历史 BOSS 邮箱附件。

## 当前不要做的事

- 不要批量处理 `boss-resumes` 里 `2026-06-03` 的 88 个历史待入库文件，用户已明确“不做处理”。
- 不要在文档中保存服务器密码或 API Key。
- 不要随意回退 Git 或服务器版本，除非用户明确指定。
- 不要改 BOSS 平台逻辑，除非任务明确要求。

## 当前待办

- 如果用户要求：提交当前未提交的简历入库稳定性补丁和本交接文档。
- 如果继续排查简历入库：优先只读检查 scan summary、batch_items、manifest，再决定是否导入。
- 如果继续改服务器：先备份到 `C:\RecruitAgent\project\rollback_backups\...`。
