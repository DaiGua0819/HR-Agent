# 2026-06-13 项目快照

## 一句话状态

- 当前项目状态：已实现 24 小时自动运转“保留浏览器、按需启动 agent、最多两个 agent 并发”的新逻辑，并修复 BOSS 中断后已处理数量被记为 0 的问题；已增加等待下一轮按钮文案、倒计时角标和总轮数设置；BOSS、51、智联 24h 处理均已改成后台任务轮询，避免长 HTTP 请求约 5 分钟断开。
- 当前服务器状态：已部署到 `C:\RecruitAgent\project`，Web 主进程已通过 `RecruitAgentWeb8765` 重启，服务器本机 `http://127.0.0.1:8765/api/automation-24h/status` 返回 200。
- 当前未完成事项：本地改动尚未 Git 提交/推送；24h 实际平台长跑效果仍需下一次启动后观察。

## 本次操作目的

- 调整 24h 自动处理策略：启动时打开并检查六个平台浏览器，登录可用的目标显示为等待处理；处理时才启动 agent，目标结束后只关闭 agent，浏览器/CDP 保持打开。
- 保持最多两个 agent 并发，避免一个平台跑完后等待另一个平台结束。
- 前端状态区区分“处理中”和“已登录待处理”。

## 修改记录

| 时间 | 类型 | 文件/目录 | 做了什么 | 目的 | 是否部署 |
| --- | --- | --- | --- | --- | --- |
| 09:30-09:55 | 修改 | `patchwork-recruit-gpt/server/services/automation24h.js` | 新增浏览器预热阶段、`browser_ready` 状态、动态 worker 队列；目标结束后日志和清理改为只关 agent | 让六个浏览器保持登录，处理进程最多两个并动态补位 | 是 |
| 09:30-09:55 | 修改 | `patchwork-recruit-gpt/server/legacy-parts/server.part005.js` | 为 24h 调度器注入 `prepareTargetBrowser()`；`ensureTargetReady()` 只在处理前启动 agent；`cleanupTarget()` 只关闭 agent | 将浏览器生命周期和 agent 生命周期拆开 | 是 |
| 09:30-09:55 | 修改 | `patchwork-recruit-gpt/server/legacy-parts/server.part001.js` | 一键启动浏览器不再提前启动 agent；状态检查不再把“浏览器在线但 agent 未开”判为异常 | 支持“登录好等待处理”的浏览器常驻状态 | 是 |
| 09:30-09:55 | 修改 | `patchwork-recruit-gpt/src/client/core/init.js` | 增加 `browser_ready`/`preparing` 文案，摘要显示已登录待处理数量，停止确认改为“关闭 agent，浏览器保持打开” | 前端展示新运行模型 | 是 |
| 09:30-09:55 | 修改 | `patchwork-recruit-gpt/src/styles/automation/platform.css` | 处理中 badge 改绿色；已登录待处理 badge 增加慢速蓝色闪烁；账号异常样式补红色 | 对齐用户要求的状态颜色 | 是 |
| 10:30-10:50 | 修改 | `patchwork-recruit-gpt/server/services/automation24h.js` | 增加中断轮次的 `recoverPartialStats` 钩子；中断日志和前端状态保留已恢复的处理数、求简历数、下载数 | 避免 BOSS agent 连接中断后 24h 统计显示处理 0 人 | 是 |
| 10:30-10:50 | 修改 | `patchwork-recruit-gpt/server/legacy-parts/server.part005.js` | 从 BOSS `recruiter_decision_log*.json` 按轮次开始时间回捞已处理候选人，并兼容 `*_accepted_resume_*`、`knowledge_answered_waiting`、`*_rejected`、`*_blocked` 等状态 | 让失败前实际发送/求简历/下载过的动作可恢复计数 | 是 |
| 10:30-10:50 | 修改 | `agent_core/legacy_parts/agent_web_server.part009.py`、`part010.py`、`part018.py` | 对处理消息接口返回做瘦身，保留 counts/state/results 摘要，减少大批量消息响应体 | 降低 HTTP 响应过大导致 `fetch failed` 或连接被中断的概率 | 是 |
| 11:00-11:10 | 修改 | `patchwork-recruit-gpt/server/services/automation24h.js`、`index.html`、`script.js`、`src/client/core/init.js`、`src/client/core/state.js`、`src/styles/automation/platform.css` | 24h 等待下一轮时按钮改为“第 n 轮已经处理完毕，等待下一轮启动中”；按钮右上角显示距离下一轮倒计时；新增“总轮数”设置，`0` 表示不限 | 让等待状态更直观，并支持指定自动运转总大轮数 | 是 |
| 14:25-14:35 | 修改 | `agent_core/legacy_parts/agent_web_server.part000.py`、`part009.py`、`part010.py`、`patchwork-recruit-gpt/server/services/automation24h.js` | BOSS 处理消息新增后台任务 `start/task/cancel` 接口；24h 调度器对 BOSS 改为启动后台任务后轮询，停止时发送 cancel 并等待当前候选人结束 | 修复 BOSS 24h 长请求超过约 5 分钟后 `fetch failed`，导致账号处理中途被误判失败的问题 | 是 |
| 15:50-15:58 | 修改 | `agent_core/legacy_parts/agent_web_server.part000.py`、`part009.py`、`part010.py`、`patchwork-recruit-gpt/server/services/automation24h.js` | 51 和智联处理消息新增后台任务 `start/task/cancel` 接口；24h 调度器将三平台统一为后台任务轮询 | 修复 51/智联同类长请求断开风险，避免消息处理中途被 `fetch failed` 误判中断 | 是 |

## 添加内容

- 24h 调度新增状态：`preparing`、`browser_ready`。
- 24h 日志新增事件：`target_browser_ready`、`target_browser_blocked`、`target_browser_failed`、`target_agent_ready`。
- 前端新增慢速蓝色闪烁动画：`automation24hBrowserReadyPulse`。
- 24h 中断日志新增部分进度恢复字段：`partialStatsRecovered`、`partialProcessed`、`partialRequestedResume`、`partialDownloadedResume`。
- BOSS 中断恢复统计新增来源：`recruiter_decision_log.json`、`recruiter_decision_log.boss_b.json`。
- 24h 设置新增字段：`maxCycles`，用于限制总大轮数；前端新增 `automation24hCountdown` 倒计时角标。
- BOSS agent 新增后台任务状态：`running`、`completed`、`failed`、`paused`；新增接口 `/api/recruiter/process-messages/start`、`/api/recruiter/process-messages/task`、`/api/recruiter/process-messages/cancel`。
- 24h 调度新增 BOSS 任务轮询逻辑：每 5 秒查询后台任务，连续轮询失败超过限制才判定异常。
- 51/智联 agent 新增处理消息后台任务接口：`/api/51job/process-messages/start|task|cancel`、`/api/zhilian/process-messages/start|task|cancel`。
- 24h 调度的后台任务轮询已泛化为三平台共用逻辑，BOSS、51、智联都会先启动任务再轮询结果。

## 删除内容

- 24h 一键浏览器启动链路中提前启动 agent 的行为。
- 24h 目标清理时关闭浏览器/CDP 的行为。

## 触碰文件和目录

- `patchwork-recruit-gpt/server/services/automation24h.js`
- `patchwork-recruit-gpt/server/legacy-parts/server.part001.js`
- `patchwork-recruit-gpt/server/legacy-parts/server.part005.js`
- `agent_core/legacy_parts/agent_web_server.part000.py`
- `agent_core/legacy_parts/agent_web_server.part009.py`
- `agent_core/legacy_parts/agent_web_server.part010.py`
- `agent_core/legacy_parts/agent_web_server.part018.py`
- `patchwork-recruit-gpt/index.html`
- `patchwork-recruit-gpt/script.js`
- `patchwork-recruit-gpt/src/client/core/init.js`
- `patchwork-recruit-gpt/src/client/core/state.js`
- `patchwork-recruit-gpt/src/styles/automation/platform.css`
- `项目修改快照/2026-06-13-项目修改快照.md`

## 服务器动作

- 服务器项目目录：`C:\RecruitAgent\project\patchwork-recruit-gpt`
- 备份路径：`C:\RecruitAgent\project\rollback_backups\automation24h_keep_browsers_20260613014931`
- 上传文件：
  - `server/services/automation24h.js`
  - `server/legacy-parts/server.part001.js`
  - `server/legacy-parts/server.part005.js`
  - `src/client/core/init.js`
  - `src/styles/automation/platform.css`
- 重启方式：停止旧 `node.exe run_server_keepalive.js` 进程 `PID=8112`，通过计划任务 `RecruitAgentWeb8765` 重新拉起，新进程 `PID=2456`。
- 服务器验证：
  - `node --check server/services/automation24h.js` 通过。
  - `node --check server.js` 通过。
  - `node --check src/client/core/init.js` 通过。
  - 服务器本机 `GET http://127.0.0.1:8765/api/automation-24h/status` 返回 200。
  - 返回 payload 中 `summary.browserReady` 字段存在，目标数为 6，当前 `status=stopped`、`active=false`。
- 10:30-10:50 增量部署：
  - 备份路径：`C:\RecruitAgent\project\rollback_backups\partial_stats_recovery_20260613024429`
  - 上传文件：`server/services/automation24h.js`、`server/legacy-parts/server.part005.js`、`agent_core/legacy_parts/agent_web_server.part009.py`、`agent_core/legacy_parts/agent_web_server.part010.py`、`agent_core/legacy_parts/agent_web_server.part018.py`
  - 重启方式：停止旧 `node.exe run_server_keepalive.js` 进程 `PID=2456`，通过计划任务 `RecruitAgentWeb8765` 重新拉起，新进程 `PID=17940`。
  - 服务器验证：`node --check patchwork-recruit-gpt/server/services/automation24h.js` 通过；server 分片拼接编译通过；agent 分片编译通过；`GET http://127.0.0.1:8765/api/automation-24h/status` 返回 200，当前 `status=stopped`、`active=false`。
- 11:00-11:10 增量部署：
  - 备份路径：`C:\RecruitAgent\project\rollback_backups\automation24h_button_cycles_20260613030149`
  - 上传文件：`server/services/automation24h.js`、`index.html`、`script.js`、`src/client/core/init.js`、`src/client/core/state.js`、`src/styles/automation/platform.css`
  - 重启方式：停止旧 `node.exe run_server_keepalive.js` 进程 `PID=17940`，通过计划任务 `RecruitAgentWeb8765` 重新拉起，新进程 `PID=12712`。
  - 服务器验证：上传文件 `node --check` 通过；server 分片拼接编译通过；`GET http://127.0.0.1:8765/api/automation-24h/status` 返回 200，当前 `status=stopped`、`active=false`，`settings.maxCycles=0`。
- 14:25-14:35 增量部署：
  - 上传文件：`agent_core/legacy_parts/agent_web_server.part000.py`、`agent_core/legacy_parts/agent_web_server.part009.py`、`agent_core/legacy_parts/agent_web_server.part010.py`、`patchwork-recruit-gpt/server/services/automation24h.js`
  - 重启方式：停止旧 `node.exe run_server_keepalive.js`，通过计划任务 `RecruitAgentWeb8765` 重新拉起。
  - 服务器验证：`node --check patchwork-recruit-gpt/server/services/automation24h.js` 通过；agent 19 个分片拼接编译通过；外部访问 `http://218.244.142.84:8080/index.html?job=HRBP` 返回 200；`GET http://218.244.142.84:8080/api/automation-24h/status` 返回 200，当前 `status=stopped`、`active=false`。
- 15:50-15:58 增量部署：
  - 上传文件：`agent_core/legacy_parts/agent_web_server.part000.py`、`agent_core/legacy_parts/agent_web_server.part009.py`、`agent_core/legacy_parts/agent_web_server.part010.py`、`patchwork-recruit-gpt/server/services/automation24h.js`
  - 重启方式：停止旧 `node.exe run_server_keepalive.js`，通过计划任务 `RecruitAgentWeb8765` 重新拉起。
  - 服务器验证：`node --check patchwork-recruit-gpt/server/services/automation24h.js` 通过；agent 19 个分片拼接编译通过；外部访问 `http://218.244.142.84:8080/index.html?job=HRBP` 返回 200；`GET http://218.244.142.84:8080/api/automation-24h/status` 返回 200，当前 `status=stopped`、`active=false`、`settings.maxCycles=0`。

## Git 状态

- 最新提交：`ff3f892 修复统计图表离线显示`
- 本地未提交：有，包含 09:30 的 24h 浏览器常驻改动、10:30 的 BOSS 中断统计恢复改动，以及本快照文档。
- 是否已推送：否，本次改动尚未提交/推送。

## 排查结论

- 服务器部署已经生效；目前 24h 自动运转未启动，不会自己开始跑。
- 新逻辑会在启动 24h 后先检查浏览器登录状态，再最多启动两个 agent 处理消息；每个目标结束后只关闭 agent，保留浏览器窗口和 CDP。

## 后续注意事项

- 下次启动 24h 后需要观察一次完整大轮，重点确认六个浏览器常驻时内存是否稳定、两个 agent 是否动态补位、51/智联登录态异常是否能正确显示。
- 下次 BOSS/51/智联账号如果再出现 `fetch failed` 或连接中断，需要先看 24h 是否已经走后台任务轮询；如果轮询接口仍能返回 `running`，不要按同步请求失败处理。
- 如果 `maxCycles` 设置为正数，跑完对应大轮后会进入 `completed`，不会再进入下一轮等待；`0` 表示继续按 24h 逻辑循环。
- 手动右侧“关闭当前程序”按钮仍会关闭浏览器和 agent，这是手动关闭入口的既有逻辑。
## 2026-06-13 约面试功能增量

- 新增简历库“约面试”能力设计：候选人行按钮先确认，再调用后端接口，按来源平台账号和平台候选人 ID 搜索候选人，固定发送 `加我微信沟通，carhhxh`。
- 新增后端接口：`POST /api/resumes/:id/interview-invite`，负责解析简历来源平台、账号、`sourceKey`、`platformCandidateId`，并调用对应 agent。
- 简历数据新增并透出字段：`platformCandidateId`、`conversationKey`、`candidateIdentityKey`、`sourceKey`、`detailUrl`、`interviewInvite`；历史简历缺少平台 ID 时前端按钮禁用，不按姓名兜底。
- agent 新增 `/api/interview-invite` 和 `/api/interview-invite/probe`：支持 dry-run 搜索 ID、核对 ID、点开联系人、定位输入框/发送按钮；正式发送会复用同一套 ID 校验路径。
- 新增独立 skill：`agent_skills/interview-scheduling/SKILL.md`，明确约面试必须按平台 ID 搜索和核对，禁止用姓名兜底。
- 当前待补充：部署到服务器后，需要对 BOSS、51、智联各跑一次 dry-run，记录各平台搜索栏、ID 核对、联系人打开和输入框定位结果。

## 2026-06-13 约面试 dry-run 与修复结论
- 已部署到服务器 `C:\RecruitAgent\project`：Web 路由已生效，A 号三个 agent 已重启到 `part018-045200`，备份路径为 `C:\RecruitAgent\project\rollback_backups\interview_part018_045200\agent_core\legacy_parts\agent_web_server.part018.py`。
- 服务器 agent 重启方式调整：远程 `schtasks /Run /S` 偶发 RPC 失败，最终通过 SSH 登录服务器后在服务器本机触发计划任务 `\RecruitAgentRunCompileInviteHttp2`，避免 SSH 会话直接启动的 agent 随会话结束退出。
- dry-run 只执行搜索、核对和定位输入框，没有填入或发送 `加我微信沟通，carhhxh`。
- BOSS 宋峰峰 dry-run：启动时先从 `https://www.zhipin.com/web/chat/search` 回到 `https://www.zhipin.com/web/chat/index`；不再点击左侧“搜索”进入人才搜索页；当前 BOSS 沟通页未暴露可用联系人搜索框，返回 `search_input_not_found`，不会误发。
- 51 宋峰峰 dry-run：识别到搜索框 `搜索姓名`，搜索测试 ID 后页面返回无结果；已修复“不再把没有找到 xxx 的提示当成 ID 命中”，返回 `candidate_id_not_verified`，不会点错联系人。
- 智联宋峰峰 dry-run：可从互动页进入搜索页并输入测试 ID；已修复“不再把搜索框/筛选条件里的关键词当成候选人命中”，返回 `candidate_id_not_verified`，不会把当前输入内容误判为已核对候选人。
- 当前限制：现有简历库 651 条记录中没有真实 `platformCandidateId/conversationKey/candidateIdentityKey/detailUrl` 可用字段，历史简历按钮会禁用；后续需要在简历下载/入库链路中尽量保存平台真实候选人 ID 或详情 URL，否则不能按用户要求“只按平台 ID 搜索，不按姓名兜底”。
- 本地 dry-run 原始记录：`C:\Users\24471\recruitagent-monitor-logs\interview-invite-dryrun-20260613-0455.json`。

## 2026-06-13 Git 上传记录
- 本次准备提交名称：`初步完成自动约面试`。
- 提交范围：简历库约面试按钮、后端约面试接口、agent 约面试 dry-run/发送入口、三平台搜索与 ID 核对保护、独立 `interview-scheduling` skill，以及本快照文档。
- 服务器状态：A 号三个 agent 已部署并保持 ready，`boss_a:8787`、`job51_a:8789`、`zhilian_a:8790` 均可访问；Web 路由 `POST /api/resumes/:id/interview-invite` 已生效。
- 验证结论：本轮只做 dry-run，没有发送约面试话术；BOSS/51/智联均已避免误命中和误发送，历史简历因缺少真实平台 ID 暂不启用约面试按钮。
