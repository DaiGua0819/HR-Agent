# 网页端智能顾问 Agent

这是终端版 CloakBrowser 控制器的网页端雏形，用来把“聊天、文件保存、页面观察、大模型规划、可视化操作、拟人化演示”串成一个本地 Agent。

## 启动

在当前目录运行：

```powershell
.\start_web_agent.ps1
```

然后打开：

```text
http://127.0.0.1:8787
```

## 使用流程

1. 保持 CloakBrowser 的 `9222` 调试端口开启。
2. 打开网页端智能顾问。
3. 先上传简历、PDF 或资料文件。
4. 在聊天框输入任务，例如：

```text
帮我上传刚才的简历，然后等待页面显示检测结果
```

Agent 会执行：

```text
观察页面 -> 大模型规划下一步 -> 调用浏览器工具 -> 可视化鼠标操作 -> 验证结果 -> 继续下一步
```

## 已有能力

- 网页聊天入口。
- 多文件上传并保存到本地 `agent_uploads/`。
- 文件注册表 `agent_files.json`，后续任务可以引用“刚才的文件”。
- 调用 CloakBrowser 页面观察。
- 调用大模型规划动作。
- 调用现有可视化鼠标、贝塞尔移动和拟人化暂停。
- 执行轨迹面板展示每一步思考、动作和结果。
- 阻止高风险点击自动执行，例如提交、删除、付款、发送等。

## 后续可以继续加强

- 把执行结果验证做得更细，例如自动识别上传成功、解析中、检测完成、错误提示。
- 给 Agent 增加任务状态机，例如 `need_file`、`ready_to_upload`、`waiting_result`、`done`。
- 加入长期记忆和用户偏好，例如默认简历、默认岗位、常用 JD。
- 增加截图预览，让网页端也能看到 CloakBrowser 当前页面状态。
